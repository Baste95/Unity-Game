using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ReticuleEvent : MonoBehaviour
{ /*  private bool isEnter = false;
    Object objectSelected;
    public AudioSource SpeakingAudio;
    public Text QuestText;

    

    public void Update()
    {
        if (isEnter == true)
        {
            if (Input.GetButtonDown("Button_X"))
            {
                Debug.Log(objectSelected.name);
                ButtonClick.Play();
            }
        }
    }

    public void EnterReticule(Object obj)
    {
        objectSelected = obj;
        isEnter = true;
        InteractText.text = "Appuyer sur X/B";
    }

    public void ExitReticule()
    {
        isEnter = false;
        InteractText.text = " ";
    }*/
 
}
